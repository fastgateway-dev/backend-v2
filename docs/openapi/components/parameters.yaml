page:
  name: page
  in: query
  schema:
    type: integer
    default: 1
    minimum: 1
limit:
  name: limit
  in: query
  schema:
    type: integer
    default: 20
    minimum: 1
    maximum: 100
userId:
  name: userId
  in: path
  required: true
  schema:
    type: string
    format: uuid
projectId:
  name: projectId
  in: path
  required: true
  schema:
    type: string
    format: uuid
teamId:
  name: teamId
  in: path
  required: true
  schema:
    type: string
    format: uuid
domainTemplateId:
  name: domainTemplateId
  in: path
  required: true
  schema:
    type: string
    format: uuid
domainId:
  name: domainId
  in: path
  required: true
  schema:
    type: string
    format: uuid
namespaceId:
  name: namespaceId
  in: path
  required: true
  schema:
    type: string
    format: uuid
routeId:
  name: routeId
  in: path
  required: true
  schema:
    type: string
    format: uuid
clientId:
  name: clientId
  in: path
  required: true
  schema:
    type: string
    format: uuid
ipId:
  name: ipId
  in: path
  required: true
  schema:
    type: string
    format: uuid
attachmentId:
  name: attachmentId
  in: path
  required: true
  schema:
    type: string
    format: uuid
approvalId:
  name: approvalId
  in: path
  required: true
  schema:
    type: string
    format: uuid
stageId:
  name: stageId
  in: path
  required: true
  schema:
    type: string
    format: uuid
headerId:
  name: headerId
  in: path
  required: true
  schema:
    type: string
    format: uuid
policyId:
  name: policyId
  in: path
  required: true
  schema:
    type: string
    format: uuid
tokenId:
  name: tokenId
  in: path
  required: true
  schema:
    type: string
    format: uuid
