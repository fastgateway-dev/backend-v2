apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-tls-nginx-conf
  namespace: default
data:
  default.conf: |
    server {
        listen 443 ssl;
        server_name backend-tls-server-1 backend-tls-server-1.default.svc.cluster.local;

        ssl_certificate /etc/nginx/ssl/tls.crt;
        ssl_certificate_key /etc/nginx/ssl/tls.key;

        location / {
            return 200 '{"status":"ok","backend":"tls","server":"backend-tls-server-1"}';
            add_header Content-Type application/json;
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-tls-server-1
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-tls-server-1
  template:
    metadata:
      labels:
        app: backend-tls-server-1
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          ports:
            - containerPort: 443
          volumeMounts:
            - name: ssl-certs
              mountPath: /etc/nginx/ssl
              readOnly: true
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d
              readOnly: true
      volumes:
        - name: ssl-certs
          secret:
            secretName: backend-tls-server-cert
        - name: nginx-conf
          configMap:
            name: backend-tls-nginx-conf
---
apiVersion: v1
kind: Service
metadata:
  name: backend-tls-server-1
  namespace: default
spec:
  selector:
    app: backend-tls-server-1
  ports:
    - protocol: TCP
      port: 443
      targetPort: 443
